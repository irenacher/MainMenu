<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.irenachernyak.mainmenu" >

    <!-- permission needed to play video -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true">

        <!-- permissions needed to use camera APIs -->
        <uses-permission android:name="android.permission.CAMERA"/>
        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>



        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="userPortrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TopicsActivity"
             android:theme="@style/AppTheme"
            android:screenOrientation="userPortrait"
            android:parentActivityName=".MainActivity">

        </activity>

        <activity android:name=".HairLossDetailsActivity"
            android:theme="@style/AppTheme"
            android:screenOrientation="userPortrait"
            android:parentActivityName=".TopicsActivity">

        </activity>

        <activity android:name=".TakeSelfieActivity"
            android:label="Visualize Your Hair Loss"
            android:theme="@style/AppTheme"
            android:screenOrientation="userPortrait"
            android:parentActivityName=".MainActivity">

        </activity>

        <activity android:name=".EvaluateNorwoodActivity"
            android:label="Visualize Your Hair Loss"
            android:theme="@style/AppTheme"
            android:screenOrientation="userPortrait"
            android:parentActivityName=".MainActivity">

        </activity>

        <activity android:name=".PageViewActivity"
            android:theme="@style/AppTheme"
            android:parentActivityName=".TopicsActivity">

        </activity>

        <activity android:name=".RequestPhotosActivity"
            android:theme="@style/AppTheme"
            android:screenOrientation="userPortrait"
            android:parentActivityName=".MainActivity">

        </activity>

        <activity android:name=".RequestFormActivity"
            android:theme="@style/AppTheme"
            android:screenOrientation="userPortrait"
            android:windowSoftInputMode="stateHidden"
            android:parentActivityName=".RequestPhotosActivity">

        </activity>

    </application>

</manifest>
